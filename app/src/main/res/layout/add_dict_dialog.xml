<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/addDD_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="10dp"
        android:text="Добавить словарь"
        android:textSize="18dp"/>

    <EditText
        android:id="@+id/addDD_name"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="10dp"
        android:paddingEnd="10dp"
        app:layout_constraintTop_toBottomOf="@id/addDD_text"
        app:layout_constraintStart_toStartOf="@id/addDD_text"
        android:hint="Название"/>

    <EditText
        android:id="@+id/addDD_desc"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:paddingEnd="10dp"
        android:paddingBottom="10dp"
        app:layout_constraintTop_toBottomOf="@id/addDD_name"
        app:layout_constraintStart_toStartOf="@id/addDD_name"
        android:hint="Описание"/>

</androidx.constraintlayout.widget.ConstraintLayout>